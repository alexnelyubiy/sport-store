import products, { initialProductsState } from './products';
import userInfo, { initialUserInfoState } from './userInfo';
import filter, { initialFilterState } from './filter';
import cart, { initialCartState } from './cart';
import pricesList, { initialPricesState } from './pricesList';
import user, { initialUserState } from './userInfo';
import fetchUserInfo, { initialFetchUserInfoState } from './fetchUserInfo'

export {
  products, initialProductsState, userInfo, initialUserInfoState, filter, initialFilterState, cart, initialCartState, pricesList, initialPricesState, user, initialUserState, initialFetchUserInfoState, fetchUserInfo
};
